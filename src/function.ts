import * as compat from 'es-toolkit/compat'
import * as est from 'es-toolkit/function'

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').after}
 */
export const after: typeof est.after = est.after

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').ary}
 */
export const ary: typeof est.ary = est.ary

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').before}
 */
export const before: typeof est.before = est.before

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').curry}
 */
export const curry: typeof est.curry = est.curry

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').curryRight}
 */
export const curryRight: typeof est.curryRight = est.curryRight

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').debounce}
 */
export const debounce: typeof est.debounce = est.debounce

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').flow}
 */
export const flow: typeof est.flow = est.flow

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').flowRight}
 */
export const flowRight: typeof est.flowRight = est.flowRight

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').identity}
 */
export const identity: typeof est.identity = est.identity

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').memoize}
 */
export const memoize: typeof est.memoize = est.memoize

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').noop}
 */
export const noop: typeof est.noop = est.noop

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').once}
 */
export const once: typeof est.once = est.once

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').partial}
 */
export const partial: typeof est.partial = est.partial

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').partialRight}
 */
export const partialRight: typeof est.partialRight = est.partialRight

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').rest}
 */
export const rest: typeof est.rest = est.rest

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').throttle}
 */
export const throttle: typeof est.throttle = est.throttle

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').unary}
 */
export const unary: typeof est.unary = est.unary

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').negate}
 */
export const negate: typeof est.negate = est.negate

/**
 * @category Function
 *
 * @alias {import('es-toolkit/function').spread}
 */
export const spread: typeof est.spread = est.spread

// ... existing imports ...

/**
 * @category Function
 *
 * @alias {import('es-toolkit/compat').attempt}
 */
export const attempt: typeof compat.attempt = compat.attempt

/**
 * @category Function
 *
 * @alias {import('es-toolkit/compat').bind}
 */
export const bind: typeof compat.bind = compat.bind

/**
 * @category Function
 *
 * @alias {import('es-toolkit/compat').bindKey}
 */
export const bindKey: typeof compat.bindKey = compat.bindKey

/**
 * @category Function
 *
 * @alias {import('es-toolkit/compat').defer}
 */
export const defer: typeof compat.defer = compat.defer

/**
 * @category Function
 *
 * @alias {import('es-toolkit/compat').flip}
 */
export const flip: typeof compat.flip = compat.flip

/**
 * @category Function
 *
 * @alias {import('es-toolkit/compat').rearg}
 */
export const rearg: typeof compat.rearg = compat.rearg
