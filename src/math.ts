import * as compat from 'es-toolkit/compat'
import * as est from 'es-toolkit/math'

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').sum}
 */
export const sum: typeof est.sum = est.sum

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').sumBy}
 */
export const sumBy: typeof est.sumBy = est.sumBy

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').clamp}
 */
export const clamp: typeof est.clamp = est.clamp

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').inRange}
 */
export const inRange: typeof est.inRange = est.inRange

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').mean}
 */
export const mean: typeof est.mean = est.mean

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').meanBy}
 */
export const meanBy: typeof est.meanBy = est.meanBy

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').range}
 */
export const range: typeof est.range = est.range

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').rangeRight}
 */
export const rangeRight: typeof est.rangeRight = est.rangeRight

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').randomInt}
 */
export const randomInt: typeof est.randomInt = est.randomInt

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').median}
 */
export const median: typeof est.median = est.median

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').medianBy}
 */
export const medianBy: typeof est.medianBy = est.medianBy

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').random}
 */
export const random: typeof est.random = est.random

/**
 * @category Math
 *
 * @alias {import('es-toolkit/math').round}
 */
export const round: typeof est.round = est.round

// ... existing imports and exports ...

/**
 * @category Math
 *
 * @alias {import('es-toolkit/compat').ceil}
 */
export const ceil: typeof compat.ceil = compat.ceil

/**
 * @category Math
 *
 * @alias {import('es-toolkit/compat').floor}
 */
export const floor: typeof compat.floor = compat.floor

/**
 * @category Math
 *
 * @alias {import('es-toolkit/compat').max}
 */
export const max: typeof compat.max = compat.max

/**
 * @category Math
 *
 * @alias {import('es-toolkit/compat').min}
 */
export const min: typeof compat.min = compat.min

/**
 * @category Math
 *
 * @alias {import('es-toolkit/compat').parseInt}
 */

// biome-ignore lint/suspicious/noShadowRestrictedNames: <explanation>
export const parseInt: typeof compat.parseInt = compat.parseInt
