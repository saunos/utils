import * as est from 'es-toolkit/promise'

/**
 * @category Promise
 *
 * @alias {import('es-toolkit/promise').delay}
 */
export const delay: typeof est.delay = est.delay

/**
 * @category Promise
 *
 * @alias {import('es-toolkit/promise').timeout}
 */
export const timeout: typeof est.timeout = est.timeout

/**
 * @category Promise
 *
 * @alias {import('es-toolkit/promise').withTimeout}
 */
export const withTimeout: typeof est.withTimeout = est.withTimeout
